'use strict';
angular.module('playerModule')
    .controller('PlayerCtrl', ['$scope', '$state', 'PlayerRepository',
        function ($scope, $state, PlayerRepository) {
            // abstract route
        }
    ]);
